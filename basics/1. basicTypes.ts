// Basic types: string, number, boolean. 
// :type (one of the basic types), for example :string means type annotation for our varibles that can include only string values
let username: string = 'Alex';
let userage: number = 30;
let isHealthy: boolean = true;